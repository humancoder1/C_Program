#include<stdio.h>  //<stdio.h> is a library in C language which contains all the functions

int main() {
    printf("Hello World");    // for printing more than one character or a word use double quotes("") 
    return 1;       // return keyword returns a value at the exit of program after it succesfully executes.
}