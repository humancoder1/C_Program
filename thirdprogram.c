#include<stdio.h>

int main(){
    int i=9,j=3,k;
    // i=i+3;      // doing incrementation of 3 in variable i
    i+=3;       //this is short hand operation , this is also incrementation of 3 in variable i
    printf("value of i is :%d \n",i);

    j*=4;
    printf("value of j is :%d",j);
}


// we cannot define main function more than once in a program